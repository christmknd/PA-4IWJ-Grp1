setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web:
      dockerfile: app/Dockerfile
      target: app_nginx
    worker:
      dockerfile: app/Dockerfile
      target: app_php
