setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    php :
      dockerfile: app/Dockerfile
      target: app_php
    nginx:
      dockerfile: app/Dockerfile
      target: app_nginx

