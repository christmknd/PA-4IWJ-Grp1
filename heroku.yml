setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    release:
      dockerfile: app/Dockerfile
      target: app_php
    web :
      dockerfile: app/Dockerfile
      target: app_nginx

