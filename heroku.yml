setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    POSTGRES_DB: db
    POSTGRES_USER: user
    POSTGRES_PASSWORD: pwd
build:
  docker:
    release:
      dockerfile: app/Dockerfile
      target: app_php
    web: app/Dockerfile
